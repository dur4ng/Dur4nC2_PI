package generate

import (
	. "github.com/onsi/ginkgo/v2"
)

var _ = Describe("Implant generator", func() {
	BeforeEach(func() {

	})
	Context("Binary build", func() {
		It("Build an executable", func() {
			//TODO need add implantConfig as parameter
			//implantPath, err := CreateImplant("127.0.0.1", "http://127.00.1:8000", Emtpy, 5, 3, Executeable)
			//Ω(err).To(Succeed())
			//fmt.Println(implantPath)
		})
	})
})
